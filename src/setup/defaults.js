const mainContent = document.getElementById("app");

const baseContent = `
  <section class="card">
    <h1>Welcome to The JavaScript Quizzes Playground</h1>
    <p>There is nothing fancy to visualize here. This is because this page is just a placeholder.</p>
    <p>Most things happen in the console</p>
    <p>See respective script if you need to test the code</p>
  </section>
`;

mainContent.innerHTML = baseContent;
